//The split() method splits a string into an array of substrings.
//Any separator can be specified. The default is comma (,).

// Return Value:
// A new array, (of strings, split at each point where the separator occurs in the given string.)

//SYNTAX
/*
string .split(separator)
string .split(separator, limit)

separator
The pattern describing where each split should occur. 
— the typical example being a regular expression.
*/

let text = "How are you doing today?";

const splitRtnVal =  text.split(" ");

console.log(splitRtnVal)
//--------------------------

//--------------------------




